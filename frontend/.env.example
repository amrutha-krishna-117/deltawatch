# Frontend environment variables
# Copy to .env.local and fill in for production deployment

# Backend URL — set this when deploying (e.g. Railway, Render)
# Leave blank to use http://localhost:4000 (local dev default)
VITE_API_URL=https://your-backend.railway.app
